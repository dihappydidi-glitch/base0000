# Десятитысячно-единичная система счисления (base-10000)
## Спецификация

---

## 1. Назначение

Десятитысячно-единичная система счисления (base-10000 / B10K) предназначена
для **представления и вычислений над сколь угодно большими десятичными числами**
без конвертации в машинный целочисленный тип (`int`).

Ключевое свойство: **разрядность не ограничена**. Число может содержать
миллионы, миллиарды и более разрядов — лишь бы хватило памяти.
Все арифметические операции выполняются поразрядно, над массивами цифр 0…9999.

---

## 2. Внешний формат строки

### 2.1. Целые числа

```
[знак]левая_половина : правая_половина
```

| Элемент | Обязателен? | Описание |
|---------|-------------|----------|
| `знак` | нет | `-` для отрицательных чисел |
| `левая_половина` | да¹ | одна или более 4-цифровых групп, разделённых `.` |
| `:` | да | ровно одно двоеточие |
| `правая_половина` | да | одна или более 4-цифровых групп, разделённых `.` |

¹ Если половина не содержит ни одной группы, она считается равной `0`.

**Правила:**
- Каждая группа — ровно 4 десятичные цифры (с ведущими нулями), значение 0…9999.
- Левая и правая половины имеют **одинаковое число групп**.
- Группы внутри половины разделяются точками `.`.
- Ровно одно двоеточие `:` между половинами.
- Минимальная запись — `0000:0000` (ноль).

### 2.2. Дробные числа

Формат — то же самое `левая_половина : правая_половина`, но с **запятой** как десятичным разделителем. Запятая может находиться **в любой половине**:

```
# целая часть в левой половине
[знак]целая_часть , левая_дробь : правая_дробь

# целая часть в правой половине
[знак]левая_дробь : целая_часть , правая_дробь
```

| Элемент | Обязателен? | Описание |
|---------|-------------|----------|
| `знак` | нет | `-` для отрицательных чисел |
| `целая_часть` | да | десятичное целое (цифры, примыкающие к запятой) |
| `,` | да | десятичный разделитель (европейская нотация) |
| `левая_дробь` / `правая_дробь` | да¹ | одна или более групп цифр, разделённых `.` |
| `:` | да | ровно одно двоеточие |

¹ Если половина не содержит ни одной группы, считается пустой.

**Правила:**
- Запятая `,` — десятичный разделитель, может быть в **любой** из двух половин.
- Целая часть — группа цифр, примыкающая к запятой в своей половине.
- Все остальные группы (в обеих половинах) участвуют в формировании дробной части.
- Группы в дробной части **не обязаны** быть 4-цифровыми — ведущие нули могут опускаться (например `70` = `0070`).
- Группы внутри половины разделяются точками `.`.
- Ровно одно двоеточие `:` между половинами.

### 2.3. Математическая модель — переплетение (interleaving)

#### Для целых чисел

Big-endian разряды переплетаются между половинами:

```
BE = [L₀, R₀, L₁, R₁, …, Lₙ₋₁, Rₙ₋₁]

       2n−1           2n−2              0
total = L₀·B    + R₀·B    + … + Rₙ₋₁·B

где B = 10000, n = число пар, L₊ — левые группы, R₊ — правые группы
```

Или эквивалентно: **чётные индексы BE → левая половина, нечётные → правая**.

#### Для дробных чисел

Общий формат: `[знак]левая_половина : правая_половина` с запятой `,` в одной из половин.

Целая часть — цифры, примыкающие к запятой. Дробная часть образуется
переплетением **групп** обеих половин (без целой части) — **по тому же принципу,
что и для целых чисел** (group-level interleaving):

1. Все группы левой половины (кроме целой части) — значения 0…9999.
2. Все группы правой половины (кроме целой части) — значения 0…9999.
3. Группы переплетаются: `BE = [L₀, R₀, L₁, R₁, …, Lₙ₋₁, Rₙ₋₁]`
4. Каждый BE[i] дополняется до 4 десятичных цифр (слева) — `pad4(BE[i])`.
5. Все 4-цифровые строки сцепляются в одну последовательность цифр.
6. Полученная строка — **десятичные цифры дробной части** (ведущие нули
   удаляются, так как они не влияют на значение).

Математически: если `s = concat(pad4(BE[0]), pad4(BE[1]), …, pad4(BE[n-1]))`,
а `s' = s.lstrip('0')`, то дробная часть = `0.s'` в десятичной системе.

Или эквивалентно: B10K хранит дробную часть не как сумму по степеням 10000,
а как **десятичные цифры**, разбитые на группы по 4 и переплетённые между
левой и правой половинами.

**Алгоритм десятичные → B10K (для дробных):**

1. Взять десятичные цифры дробной части как строку (без ведущего нуля).
2. Дополнить строку слева нулями до длины, кратной 4.
3. Разбить на группы по 4 цифры.
4. Группы на чётных позициях → левая половина.
5. Группы на нечётных позициях → правая половина.
6. В первой группе левой половины убрать ведущие нули.
7. Собрать строку: `целая,левая : правая`.

### 2.4. Примеры строк

**Целые:**

| Строка | BE-цифры | Всего |
|--------|----------|-------|
| `0000:0005` | [0, 5] | 5 |
| `0001:0000` | [1, 0] | 10 000 |
| `9999:9999` | [9999, 9999] | 99 999 999 |
| `0000.0001:0000.0000` | [0, 0, 1, 0] | 10 000 |
| `0000.0000:0001.0000` | [0, 1, 0, 0] | 100 000 000 |
| `9999.9999:0000.0000` | [9999, 0, 9999, 0] | 9 999 000 099 990 000 |
| `-0000:0005` | [0, 5] | −5 |

**Дробные:**

| Строка | Десятичное | Запятая в |
|--------|------------|-----------|
| `6,708.3249.0892.1006:2039.9369.2752.1938` | 6,708203932499369089… (√45) | левой половине |
| `3,556.0449.8060.4622:8933.0062.0615.2389` | 3,556893304490062806… (∛45) | левой половине |

Разбор `6,708.3249.0892.1006:2039.9369.2752.1938` (запятая в левой половине):

```
  целая_часть      левая половина               правая половина
 ┌──────────┐  ┌──────────────────────────┐  ┌──────────────────────────┐
 6          ,  708 . 3249 . 0892 . 1006  :  2039 . 9369 . 2752 . 1938
```

1. Целая часть: `6`.
2. Дробные группы левой половины (кроме целой части, значения 0…9999):
   L = [708, 3249, 892, 1006]
3. Дробные группы правой половины (значения 0…9999):
   R = [2039, 9369, 2752, 1938]
4. Переплетение групп (group-level interleaving):
   BE = [708, 2039, 3249, 9369, 892, 2752, 1006, 1938]
5. Каждая группа дополняется до 4 цифр:
   "0708", "2039", "3249", "9369", "0892", "2752", "1006", "1938"
6. Сцепление: "07082039324993690892275210061938"
7. Удаление ведущего нуля: "7082039324993690892275210061938"
8. Десятичная дробь: 0,7082039324993690892275210061938
   Число: 6,7082039324993690892275210061938 = √45

Разбор `3,556.0449.8060.4622:8933.0062.0615.2389` (запятая в левой половине):

```
  целая_часть      левая половина              правая половина
 ┌──────────┐  ┌──────────────────────────┐  ┌──────────────────────────┐
 3          ,  556 . 0449 . 8060 . 4622  :  8933 . 0062 . 0615 . 2389
```

1. Целая часть: `3`.
2. Дробные группы левой половины (кроме целой части, значения 0…9999):
   L = [556, 449, 8060, 4622]
3. Дробные группы правой половины (значения 0…9999):
   R = [8933, 62, 615, 2389]
4. Переплетение групп (group-level interleaving):
   BE = [556, 8933, 449, 62, 8060, 615, 4622, 2389]
5. Каждая группа дополняется до 4 цифр:
   "0556", "8933", "0449", "0062", "8060", "0615", "4622", "2389"
6. Сцепление: "05568933044900628060061546222389"
7. Удаление ведущего нуля: "5568933044900628060061546222389"
8. Десятичная дробь: 0,5568933044900628060061546222389
   Число: 3,5568933044900628060061546222389 = ∛45

### 2.5. Каноническая форма (целые числа)

При форматировании система приводит число к **канонической форме**:

1. Little-endian разряды → big-endian
2. Дополнить до чётного числа групп ведущим `0` при необходимости
3. Разбить на пары: (BE[0], BE[1]), (BE[2], BE[3]), …
4. Первый элемент каждой пары → левая половина, второй → правая
5. Каждая группа → `%04d`, соединить через `.` внутри половины, половины через `:`

---

## 3. Внутреннее представление

Число хранится как структура:

```python
@dataclass
class B10K:
    sign: int        # 1 или -1
    digs: List[int]  # little-endian массив разрядов 0..9999
```

### 3.1. Little-endian разряды

Массив `digs` — это цифры числа в системе с основанием 10 000,
записанные от младшего разряда к старшему:

```
digs[0] + digs[1]·B¹ + digs[2]·B² + … + digs[n-1]·Bⁿ⁻¹
```

где B = 10000.

### 3.2. Сокращение (trim)

Массив не содержит ведущих нулей (кроме самого нуля: `[0]`).
Это значит, что `digs[-1] ≠ 0` для любого ненулевого числа.

### 3.3. Знак

- `sign = 1` для положительных чисел и нуля
- `sign = -1` для отрицательных чисел
- Ноль всегда `sign = 1`

---

## 4. Формат-пакет: структура файла

```
base10000.py
├── B10K (dataclass)
│   ├── sign, digs
│   ├── __add__, __sub__, __mul__, …  (операторы Python)
│   ├── __eq__, __lt__, __gt__, …     (сравнения)
│   ├── __str__, __repr__              (строки)
│   └── __hash__, __bool__
│
├── Вспомогательные
│   ├── _from_int(n)        — Python int → B10K (для тестов)
│   ├── _trim(digs)         — удаление ведущих нулей
│   ├── _is_zero(a)         — проверка на ноль
│   └── _cmp_abs(a, b)      — сравнение модулей
│
├── Поразрядные операции (над |a|)
│   ├── _add_abs(a, b)      — сложение
│   ├── _sub_abs(a, b)      — вычитание (a ≥ b)
│   ├── _mul_abs(a, b)      — умножение столбиком
│   ├── _mul_small(a, d)    — умножение на цифру
│   ├── _div_mod_abs(a, b)  — деление уголком
│   ├── _shift_left_abs(a, k)  — сдвиг влево на k разрядов
│   └── _shift_right_abs(a, k) — сдвиг вправо на k разрядов
│
├── Основные операции
│   ├── add(a, b) / sub(a, b)
│   ├── mul(a, b)
│   ├── div_mod(a, b) → (q, r)
│   ├── div(a, b) / mod(a, b)
│   └── neg(a) / abs(a)
│
├── Дополнительные операции
│   ├── pow_b10k(a, b)
│   ├── fact(n)
│   ├── gcd(a, b) / lcm(a, b)
│   ├── shift_left(a, k) / shift_right(a, k)
│   └── isqrt(a)
│
├── Парсинг и форматирование
│   ├── parse(s)        — строка → B10K
│   └── format_num(a)   — B10K → строка
│
├── Псевдонимы
│   ├── B(s) = parse(s)
│   └── _(a) = format_num(a)
│
└── REPL-калькулятор _repl()
```

---

## 5. Поразрядные операции (алгоритмы)

### 5.1. Сложение `_add_abs(a, b)`

```
res = []
carry = 0
для i от 0 до max(len(a), len(b))-1:
    сумма = a[i] + b[i] + carry    (0, если i за границей)
    res[i] = сумма % 10000
    carry = сумма // 10000
если carry: res.append(carry)
вернуть _trim(res)
```

Сложность: O(N), где N = max(|a|, |b|).

### 5.2. Вычитание `_sub_abs(a, b)` (a ≥ b)

```
res = []
borrow = 0
для i от 0 до len(a)-1:
    разность = a[i] - (b[i] или 0) - borrow
    если разность < 0:
        разность += 10000; borrow = 1
    иначе: borrow = 0
    res[i] = разность
вернуть _trim(res)
```

Сложность: O(N).

### 5.3. Умножение `_mul_abs(a, b)` — столбиком

```
res = [0] × (len(a) + len(b))
для i, da в a:
    carry = 0
    для j, db в b:
        произведение = res[i+j] + da·db + carry
        res[i+j] = произведение % 10000
        carry = произведение // 10000
    если carry: res[i+len(b)] += carry
вернуть _trim(res)
```

Сложность: O(N·M) = O(N²) для чисел равной длины.

### 5.4. Деление `_div_mod_abs(a, b)` — уголком

```
a_be = reversed(a)    # big-endian для обхода
b_top = b[-1]         # старший разряд делителя

quot = []             # частное (big-endian)
rem_be = []           # остаток (big-endian)

для da в a_be:
    rem_be.append(da)
    если |rem_be| < |b|:
        quot.append(0); continue
    
    # прикидка цифры частного
    r_hi = rem_be[0] × 10000 + rem_be[1] (если есть)
    guess = r_hi // b_top (не больше 9999)
    
    # коррекция: подбор максимального guess,
    # при котором guess·b ≤ rem
    пока guess·b > rem: guess -= 1
    
    rem = rem - guess·b
    quot.append(guess)

вернуть (reversed(quot), rem)
```

Сложность: O(N·M), в среднем O(N).

### 5.5. Сдвиги

| Операция | Действие | LE-реализация |
|----------|----------|---------------|
| `shift_left(a, k)` | a × 10000ᵏ | `[0]×k + a.digs` |
| `shift_right(a, k)` | a // 10000ᵏ | `a.digs[k:]` |

Сдвиг влево — дописывает нули МЛАДШИМИ разрядами (умножение на степень основания).
Сдвиг вправо — отбрасывает k младших разрядов (целочисленное деление на степень основания).

---

## 6. Основные операции

### 6.1. neg(a) — смена знака

Если `a = 0`, возвращает `0`. Иначе возвращает число с тем же модулем и обратным знаком.

### 6.2. add(a, b)

```
если знаки совпадают:
    результат = (знак, _add_abs(a, b))
иначе:
    сравнить модули
    если |a| = |b|: вернуть 0
    если |a| > |b|: (знак a, _sub_abs(a, b))
    иначе:          (знак b, _sub_abs(b, a))
```

### 6.3. sub(a, b)

`sub(a, b) = add(a, neg(b))`

### 6.4. mul(a, b)

```
если a = 0 или b = 0: вернуть 0
знак: + если знаки совпадают, − если разные
результат: (знак, _mul_abs(a, b))
```

### 6.5. div_mod(a, b) — Евклидово деление

Гарантирует:
```
a = q·b + r, где 0 ≤ r < |b|
```

Алгоритм:
1. Вычислить частное и остаток через `_div_mod_abs(|a|, |b|)`
2. Частное = `sign(a)·sign(b)`, остаток = `sign(a)`
3. **Евклидова коррекция**: если остаток отрицательный → вычесть 1 из частного,
   прибавить `|b|` к остатку

### 6.6. pow_b10k(a, b) — возведение в степень

Бинарное возведение (exponentiation by squaring) для целого неотрицательного
показателя:

```
result = 1
base = a
exp = b
пока exp > 0:
    если exp нечётный: result ×= base
    base ×= base
    exp //= 2
вернуть result
```

Сложность: O(log₂(b)) умножений.

---

## 7. Дополнительные операции

### 7.1. fact(n) — факториал

```
n! = 1 × 2 × 3 × … × n
```

### 7.2. gcd(a, b) — наибольший общий делитель

Алгоритм Евклида:
```
пока b ≠ 0: a, b = b, a mod b
вернуть a
```

### 7.3. lcm(a, b) — наименьшее общее кратное

```
lcm(a, b) = |a·b| / gcd(a, b)
```

### 7.4. isqrt(a) — целочисленный квадратный корень

Метод Ньютона (вавилонский метод):

1. Начальное приближение: подбираем `x` так, что `x² > a`
2. `x = (x + a/x) / 2` до сходимости
3. Коррекция: уменьшаем, пока `x² > a`; увеличиваем, пока `(x+1)² ≤ a`

---

## 8. Парсинг и форматирование

### 8.1. parse(s) — строка → B10K

1. Удалить BOM и нецифровые символы в начале
2. Извлечь знак (`−` в начале)

**Для целых чисел (без запятой):**
3. Разделить по `:` на левую и правую половины
4. Разбить каждую половину по `.` на 4-цифровые группы
5. Валидировать каждую группу: 0…9999
6. Переплести: `BE = [L₀, R₀, L₁, R₁, …]`
7. Перевернуть в LE: `digs = reversed(BE)`, обрезать ведущие нули
8. Вернуть `B10K(sign, digs)`

**Для дробных чисел (с запятой):**
3. Разделить строку по `:` на левую и правую половины
4. Определить, в какой половине находится запятая `,`
5. **Если запятая в левой половине:**
   - Разделить левую половину по `,` на целую часть (слева) и левую дробь (справа)
   - Целая часть → десятичное целое
   - Левая дробь + правая половина → последовательности цифр для переплетения
6. **Если запятая в правой половине:**
   - Разделить правую половину по `,` на целую часть (слева от запятой) и правую дробь (справа)
   - Целая часть → десятичное целое
   - Левая половина + правая дробь → последовательности цифр для переплетения
7. Каждую группу разбить по `.` на группы цифр, склеить цифры в последовательности L и R
8. Переплести цифры L и R в 4-значные десятичные группы по схеме:
   - i чётное: 2 цифры из L + 2 цифры из R
   - i нечётное: 2 цифры из R + 2 цифры из L
9. Полученные 4-значные группы → big-endian разряды дробной части
10. Комбинировать: `total_B10K = целая · 10000ᵏ + дробные_BE` (k = число дробных групп)

### 8.2. format_num(a) — B10K → строка

1. Если 0 → `"0000:0000"`

**Для целых чисел:**
2. LE-разряды → BE: `be = reversed(a.digs)`
3. Дополнить до чётного числа групп: `be.insert(0, 0)` если нечётное
4. Разбить на пары: левые = чётные индексы BE, правые = нечётные
5. Каждую группу → `f"{g:04d}"`, соединить через `.` внутри половины
6. Соединить половины через `:`

**Для дробных чисел (расширение — в разработке):**
2. Отделить целую часть от модуля числа
3. Остаток разложить на дробные разряды (4-значные десятичные группы)
4. Переплести разряды обратно в левую и правую половины (распределяя по 2 цифры
   от каждого разряда в каждую половину)
5. Поместить запятую в выбранную половину (левую или правую) после целой части
6. Собрать строку: `половина_с_целой , остаток_половины : другая_половина`

---

## 9. REPL-калькулятор

Запуск:

```
python base10000.py --repl
```

Интерактивный режим поддерживает:
- Бинарные операции: `+`, `-`, `*`, `/`, `//`, `%`, `**`
- Функции: `fact(n)`, `gcd(a,b)`, `lcm(a,b)`, `isqrt(n)`
- Переменные: `x = 0001:0000`
- Выход: `.exit` или Ctrl+C

---

## 10. Сравнение чисел

Сравнение сначала по знаку, затем по модулю.
Сравнение модулей — сравнение длины LE-массива, затем поразрядно от старшего
к младшему.

| Операция | Реализация |
|----------|------------|
| `a == b` | знаки равны И модули равны |
| `a < b`  | если знаки разные: минус < плюс |
|          | если знаки равны: |a| < |b| × sign |
| `a > b`  | `not (a <= b)` |

---

## 11. Особые случаи

| Ситуация | Поведение |
|----------|-----------|
| Деление на ноль | `ZeroDivisionError` |
| Отрицательная степень | `ValueError` |
| Квадратный корень из отрицательного | `ValueError` |
| Факториал отрицательного | `ValueError` |
| Пустая строка при парсинге | `ValueError` |
| Группа вне 0…9999 | `ValueError` |
| Ноль (0) | `B10K(sign=1, digs=[0])` → `"0000:0000"` |
| Сдвиг нуля | возвращает 0 |
| Результат 0 после операции | знак всегда +1 |

---

## 12. Примеры вычислений

```
# Целые
0000:0005 + 0000:0003                         =  0000:0008
9999:9999 + 0000:0001                         =  0000.0000:0001.0000
0001:0000 - 0000:0001                         =  0000:9999
0000:9999 × 0000:9999                         =  9998:0001
9999:9999 × 9999:9999                         =  9999.0000:9998.0001
0001:0000 / 0000:0003                         =  0000:3333
0000:0002 ** 0000:0010                        =  0000:1024  (2¹⁰)
-0000:0007 // 0000:0003                       =  -0000:0003  r = 0000:0002  (Евклид)
fact(0000:0005)                               =  0000:0120  (5!)
isqrt(0000:0144)                              =  0000:0012  (√144)
gcd(0000:0012, 0000:0008)                     =  0000:0004
9999.9999:0000.0000 × 9999.9999:0000.0000     =  9998.9996.9998.0000:0002.0002.0001.0000

# Дробные (представление — планируется)
# √45 (запятая в левой половине): 6,708.3249.0892.1006:2039.9369.2752.1938 ≈ 6,7082039325…
# ∛45 (запятая в левой половине): 3,556.0449.8060.4622:8933.0062.0615.2389 ≈ 3,5568933044…
```

---

## 13. Ограничения

- Дробные числа поддерживаются в формате `целая , левая : правая` с переплетением цифр
- Парсинг и форматирование дробных чисел — в стадии разработки
- Степень поддерживается только для неотрицательного показателя
- Производительность: O(N²) для умножения и деления длинных чисел
- Все значения хранятся в памяти как Python-списки int'ов

---

*Документ создан 2026-07-05. Версия 3.1 — переплетающаяся модель (interleaving) с дробными числами.*
